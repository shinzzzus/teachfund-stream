# API

Base URL: http://localhost:4000

- GET /api/health
- GET /api/records
- POST /api/records
- GET /api/payments/quote

The quote route returns x402-style payment metadata for agentic payment clients.
